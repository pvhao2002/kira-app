# Deprecated — dùng scripts/nginx-infra.conf (gồm rabbit.kira.id.vn + portainer.kira.id.vn)
# Copy: sudo cp scripts/nginx-infra.conf /etc/nginx/conf.d/kira-infra.conf
# Test:  sudo nginx -t && sudo systemctl reload nginx
