Extract transaction data from this image and return ONLY valid JSON (no markdown, no explanation) with exactly this schema: {"datetime":"", "money":"", "text":"", "type":""}. Rules: type must be one of withdraw, deposit, bonus. If the image contains multiple withdraw rows (e.g., 2 lines), return only the latest/newest withdraw row as a single JSON object. If a field is missing, use an empty string