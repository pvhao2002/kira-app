# Expo SDK 52+ breaking changes (stub)

This file is a **placeholder** for Expo-specific deltas (JSC in Expo Go, edge-to-edge, module deprecations).

- Start with `SKILL.md` sections: **Expo SDK 52+ Specifics**, **Android Edge-to-Edge**, and known issues (e.g. `expo-file-system`, `expo-av`).
- Changelog index: https://expo.dev/changelog

Add notes per SDK bump (53, 54, 55) for this repo.
